export type CheckPassword = string | number;
export type String = string;

export interface UserEntry {
  name: string;
  lastName: string;
  age: number;
  mobile: number;
  city: string;
  email: string;
}
